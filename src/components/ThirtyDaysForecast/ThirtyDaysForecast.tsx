import React from 'react';
import {useAppSelector} from '../../App/app/hooks';

const ThirtyDaysForecast = () => {

    return (
        <div style={{background:'whitesmoke'}}>
            THIRTY DAYS FORECAST
        </div>
    );
};
